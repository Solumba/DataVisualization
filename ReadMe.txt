You will need to Download MLxtend and install the library in Anaconda. Instructions are found here.
https://anaconda.org/conda-forge/mlxtend for Apriori------ clustering
To install this package run one of the following:
conda install -c conda-forge mlxtend
conda install -c "conda-forge/label/cf201901" mlxtend
conda install -c "conda-forge/label/cf202003" mlxtend
conda install -c "conda-forge/label/gcc7" mlxtend

pip install --upgrade scikit-learn
pip install --upgrade matplotlib
pip install xlrd

Compare clustering results with and without scaling

See the following.
Logistic Regression 
Vector Autoregression
Autoregression